import 'package:flutter/material.dart';

class DailyNewsViewer extends StatelessWidget {
  const DailyNewsViewer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}